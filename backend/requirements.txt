fastapi[standard]==0.139.1
uvicorn[standard]==0.32.1
pydantic==2.10.3
pydantic-settings==2.7.0
sqlalchemy[asyncio]==2.0.36
alembic==1.14.0          # schema migrations (run on startup; auto-stamps existing DBs)
asyncpg==0.31.0          # async driver for the app engine
psycopg2-binary==2.9.10  # sync driver APScheduler's SQLAlchemyJobStore needs on Postgres
aiosqlite==0.20.0
httpx==0.28.1
json-repair>=0.61,<1  # salvage near-JSON from the LLM (missing quotes, trailing commas)
feedparser>=6.0        # RSS/Atom parsing for Business source fetchers (demo)
beautifulsoup4>=4.12   # generic-page extraction for Business source fetchers (demo)
certifi>=2024.0.0
truststore>=0.10.0
authlib>=1.3          # OAuth 1.0a signing for X/Twitter publishing
pillow==11.0.0
python-multipart==0.0.19
python-dotenv==1.0.1
cryptography==49.0.0
pyjwt>=2.8          # JWT sessions (multi-tenant auth)
argon2-cffi>=23     # password hashing
email-validator>=2  # pydantic EmailStr for registration
slowapi>=0.1.9      # per-IP / per-user rate limiting
sentry-sdk[fastapi]>=2  # optional error monitoring (gated by SENTRY_DSN)
python-telegram-bot==21.9
aiofiles==24.1.0
pywebview>=5.0
apscheduler>=3.10,<4
imageio>=2.37.3
imageio-ffmpeg>=0.6.0
numpy>=1.26
pytest==8.3.4
pytest-asyncio==0.25.0
pytest-httpx==0.36.2
respx==0.21.1
